apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: asgard-bifrost-bootstrap-resource
  namespace: argocd
  labels:
    app.kubernetes.io/name: asgard-bifrost-bootstrap-resource
    app.kubernetes.io/part-of: asgard-bifrost
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: default
  source:
    path: asgard-bifrost-bootstrap/bootstrap-resources
    repoURL: https://github.com/baseonballs/jdev-asgard-gitops.git
    targetRevision: HEAD
    helm:
      parameters:
        - name: domain
          value: {{ .Values.domain }}
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
